# 依赖与构建产物
node_modules
.next
out

# 环境变量（含数据库密码，禁止入库）
.env
.env.local
*.local

# 上传的文件（运行时生成）
uploads

# 日志
*.log

# 增量编译缓存（运行时生成，不入库）
*.tsbuildinfo

# Next.js 构建/开发产物（distDir 指向的目录，运行时生成，禁止入库）
.next
.next_dev/
.next_run/
.next_run2/